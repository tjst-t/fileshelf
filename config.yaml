server:
  listen: ":8080"
  dev_mode: true
  dev_user: "ubuntu"
  static_dir: "./frontend/dist"

helper:
  path: "./bin/fileshelf-helper"
  timeout: 30s

shares:
  - name: "home"
    path: "/home/ubuntu"
